# 实施计划审计报告

**文档：** 2026-04-02-eaa-frontend-implementation.md
**审计基准：** brainstorming 阶段 13 条评审意见
**审计日期：** 2026-04-02

---

## 执行摘要

实施计划**部分覆盖**了 brainstorming 阶段的评审意见：
- ✅ **P0-1, P0-2, P0-3**：核心架构问题已妥善解决
- ✅ **P1-5, P1-6**：关键工作流摩擦点已解决
- ⚠️ **P1-4**：续写行为未明确说明
- ❌ **P1-7**：多维标签未在数据结构中体现
- ⚠️ **P1-8**：灵感模式 vs 执行模式未明确
- ⚠️ **P1-9**：发布向导自动读取 traces/ 需更详细说明
- ⚠️ **P2-10, P2-11, P2-12, P2-13**：交互细节部分覆盖

---

## 详细审计

### P0-1: 灵感库 AI 自动分类的可靠性陷阱 ✅

**评审意见：**
- 需要置信度指示
- 低置信度默认「待分类」
- 支持手动纠正

**计划覆盖情况：**

| 项目 | 覆盖 | 位置 |
|------|------|------|
| 置信度字段（IdeaNote.confidence）| ✅ | 第 773 行 |
| 分类阈值处理（>=0.8 自动，0.5-0.8 建议）| ✅ | 第 1952-1954 行 |
| 分类状态指示器 UI | ✅ | 第 1941 行 |
| IAClassifier 组件 | ✅ | 第 1953 行 |

**结论：** ✅ **完全覆盖**

---

### P0-2: 回传队列的「不可见执行」危险 ✅

**评审意见：**
- 执行反馈（status + affected_files）
- 串行失败时暂停后续
- 用户可撤销/编辑

**计划覆盖情况：**

| 项目 | 覆盖 | 位置 |
|------|------|------|
| Command.status 字段（pending/executing/success/failed）| ✅ | 第 733 行 |
| Command.affected_files 字段 | ✅ | 第 734 行 |
| CommandQueuePanel 组件 | ✅ | 第 1190 行 |
| CommandQueueService 后端服务 | ✅ | 第 1158 行 |
| 执行反馈解析 | ✅ | 第 1466 行 |
| 验收标准：affected_files 可见 | ✅ | 第 1266 行 |

**缺失：**
- ❌ 串行失败时暂停后续执行逻辑未详细说明
- ❌ 队列编辑（拖拽排序、撤销）未明确

---

### P0-3: 三栏布局对「创作高峰期」的拥挤 ✅

**评审意见：**
- 支持「工作模式」切换
- 动态重组三栏
- 规则设计/小说创作/平衡调整三种模式

**计划覆盖情况：**

| 项目 | 覆盖 | 位置 |
|------|------|------|
| WorkModeSwitcher 组件 | ✅ | 第 565、105、1534 行 |
| useWorkMode Hook | ✅ | 第 630、123 行 |
| 工作模式增强（Phase 3）| ✅ | 第 1965 行 |
| 验收标准：不同任务有优化布局 | ✅ | 第 2907 行 |

**结论：** ✅ **完全覆盖**

---

### P1-4: 「继续写」vs「拆分提交」的决策真空 ⚠️

**评审意见：**
- 续写结果生成新便签（不覆盖原便签）
- 标记「AI 续写」
- 区分「草稿建议」和「待审确认」

**计划覆盖情况：**

| 项目 | 覆盖 | 位置 |
|------|------|------|
| IdeaNote.status 包含 'ai_generated' | ✅ | 第 772、280 行 |
| ai_generated 状态存在 | ✅ | 第 280 行 |

**缺失：**
- ❌ 续写结果是否生成新便签未明确
- ❌ 「草稿建议」vs「待审确认」的 UI 区分未说明
- ❌ 续写与「拓展」功能的区别未定义

**建议：** 在 Phase 1 的 Novel Assistant 实施细节中明确续写行为

---

### P1-5: 术语表「可视化编辑」与 YAML 同步的断点 ✅

**评审意见：**
- 修改前显示影响文件数
- 确认后生成批量替换计划
- 自动触发 consistency_check

**计划覆盖情况：**

| 项目 | 覆盖 | 位置 |
|------|------|------|
| TerminologyEditor 模块 | ✅ | 第 2048 行 |
| 影响分析功能 | ✅ | 第 2065 行 |
| 批量替换功能 | ✅ | 第 2072 行 |
| TerminologyService 服务 |后端| 第 2098 行 |

**结论：** ✅ **完全覆盖**

---

### P1-6: 「世界观快查」面板的侧边栏限制 ✅

**评审意见：**
- 预留「拖出独立窗口」能力
- 关键词搜索
- inline 注解（术语悬浮）

**计划覆盖情况：**

| 项目 | 覆盖 | 位置 |
|------|------|------|
| WorldviewLookup 模块 | ✅ | 第 2103 行 |
| Hover definitions（悬浮定义）| ✅ | 第 484 行 |
| Quick search（快捷搜索）| ✅ | 第 484 行 |
| Floating window（浮动窗口）| ✅ | 第 484 行 |
| Cross-refs（交叉引用）| ✅ | 第 484 行 |

**结论：** ✅ **完全覆盖**

---

### P1-7: 「模糊内容」的混合类型处理 ❌

**评审意见：**
- 支持多维标签（规则 + 小说）
- 回传时自动分叉双路检查

**计划覆盖情况：**

| 项目 | 覆盖 | 位置 |
|------|------|------|
| IdeaNote.tags 字段（字符串数组）| ⚠️ | 第 779 行（存在但语义未明确） |
| 多维标签处理逻辑 | ❌ | 未找到 |

**缺失：**
- ❌ tags 字段是否支持同时标记「规则」和「小说」未说明
- ❌ 回传时的双路分叉检查逻辑未定义
- ❌ 混合标签便签的 UI 展示未说明

**建议：** 在 Phase 3 增加多维标签支持说明

---

### P1-8: 创作节奏被工作流打断 ⚠️

**评审意见：**
- 「灵感模式」vs「执行模式」双轨
- 灵感模式只记录不生成计划
- 支持异步确认

**计划覆盖情况：**

| 项目 | 覆盖 | 位置 |
|------|------|------|
| 工作模式切换存在 | ✅ | P0-3 已确认 |

**缺失：**
- ❌ 「灵感模式」vs「执行模式」的语义未明确
- ❌ 模式之间的计划生成行为差异未说明
- ❌ 异步确认机制未定义

**建议：** 在 WorkModeSwitcher 实施时明确两种模式的行为差异

---

### P1-9: 版本发布流程的「单人作者陷阱」 ⚠️

**评审意见：**
- 自动读取 traces/ 和 CHANGELOG 生成草稿
- 根据发布类型动态调整检查

**计划覆盖情况：**

| 项目 | 覆盖 | 位置 |
|------|------|------|
| VersionRelease 模块 | ✅ | 第 2124 行 |
| Changelog draft（变更日志草稿）| ✅ | 第 2124 行 |

**缺失：**
- ⚠️ 自动读取 traces/ 和 CHANGELOG 的机制存在，但未详细说明
- ❌ 发布类型（热修复/小更新/大版本）的动态检查未说明

**建议：** 在 VersionRelease 实施细节中补充自动读取机制

---

### P2-10: 回传队列的「撤销」和「编辑」能力 ⚠️

**评审意见：**
- 队列指令可编辑
- 支持拖拽排序
- 撤销按钮
- 队列模板

**计划覆盖情况：**

| 项目 | 覆盖 | 位置 |
|------|------|------|
| CommandQueuePanel 组件 | ✅ | 第 1190 行 |
| Command.status 字段 | ✅ | 已存在 |

**缺失：**
- ❌ 指令编辑 UI 未说明
- ❌ 拖拽排序功能未提及
- ❌ 撤销按钮未明确
- ❌ 队列模板功能未定义

**建议：** 在 CommandQueuePanel 实施时补充这些交互细节

---

### P2-11: Harness 脚本输出在前端的可见性 ⚠️

**评审意见：**
- 执行日志面板
- 实时更新执行状态
- 失败指令的错误显示

**计划覆盖情况：**

| 项目 | 覆盖 | 位置 |
|------|------|------|
| Execution log panel（执行日志面板）| ✅ | 第 1568 行 |
| WebSocket 实时通信 | ✅ | 第 74 行 |
| Command.affected_fields 解析 | ✅ | 第 1466 行 |

**缺失：**
- ⚠️ TRACE 记录的实时显示未详细说明
- ⚠️ 错误信息的 UI 展示未明确

**建议：** 在 ExecutionLogPanel 实施时补充 TRACE 显示逻辑

---

### P2-12: Markdown 编辑器与 AI 建议的集成 ⚠️

**评审意见：**
- 建议面板（非侵入式）
- 应用后标记「AI 辅助」
- 风格检查状态指示灯

**计划覆盖情况：**

| 项目 | 覆盖 | 位置 |
|------|------|------|
| Monaco Editor | ✅ | 第 67 行 |
| NovelAssistant 模块 | ✅ | 第 108 行 |
| ai_generated 状态 | ✅ | 已存在 |

**缺失：**
- ❌ AI 建议面板组件未明确
- ❌ 风格检查状态指示灯 UI 未说明
- ❌ 非侵入式建议的交互方式未定义

**建议：** 在 NovelAssistant 实施时补充建议面板

---

### P2-13: 文件树导航的「创意流」支持 ⚠️

**评审意见：**
- 书签功能
- 快速搜索
- 记忆上次目录

**计划覆盖情况：**

| 项目 | 覆盖 | 位置 |
|------|------|------|
| FileTree 组件 | ✅ | 第 104、563 行 |
| useFileTree Hook | ✅ | 第 122 行 |

**缺失：**
- ❌ 书签功能未提及
- ❌ 快速搜索未明确
- ❌ 上次目录记忆未说明

**建议：** 在 FileTree 实施时补充这些功能

---

## 总体评估

| 优先级 | 项目数 | 完全覆盖 | 部分覆盖 | 未覆盖 |
|--------|--------|----------|----------|--------|
| **P0** | 3 | 3 | 0 | 0 |
| **P1** | 6 | 2 | 3 | 1 |
| **P2** | 4 | 0 | 4 | 0 |
| **合计** | 13 | 5 | 7 | 1(关键) |

---

## 建议修改

### 1. 立即修改（P1-7：多维标签）

在 `IdeaNote` 接口和 Phase 3 中补充：

```typescript
interface IdeaNote {
  id: string;
  content: string;
  tags: string[];  // 支持同时标记 'rule' 和 'novel'
  status: 'active' | 'classified' | 'ai_generated';
  confidence: number;
  // 新增：混合标签处理
  isHybrid?: boolean;  // 是否包含规则和小说双标签
  created_at: string;
  last_modified: string;
  position: { x: number; y: number };
}
```

### 2. 实施时明确（P1-4, P1-8, P1-9）

在相应模块的实施步骤中补充：

- **NovelAssistant**：明确续写生成新便签，不覆盖原便签
- **WorkModeSwitcher**：明确「灵感模式」vs「执行模式」的行为差异
- **VersionRelease**：详细说明自动读取 traces/ 和 CHANGELOG 的机制

### 3. Phase 4 补充（P2 系列）

在 Phase 4 的打磨优化阶段增加：

- CommandQueuePanel：拖拽排序、撤销、编辑、模板
- ExecutionLogPanel：TRACE 实时显示、错误详情
- NovelAssistant：AI 建议面板、风格检查指示灯
- FileTree：书签、快速搜索、目录记忆

---

## 结论

实施计划**总体良好**，P0 问题已完全解决，P1 问题大部分有方案。但需要在以下方面补充：

1. **P1-7（多维标签）**：需要明确的数据结构和处理逻辑
2. **交互细节（P2 系列）**：需要在 Phase 4 中详细说明
3. **行为语义**：续写、工作模式切换等关键交互需要明确说明

建议在开始实施前，先补充这些细节到计划文档中。
